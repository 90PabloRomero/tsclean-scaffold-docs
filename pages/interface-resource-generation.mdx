# Interface Resource Generation

The command **`scaffold create:interface-resource`** generates an interface, this task has **`--name`** and **`--resource`** as parameters this is required.
The name must be in lower case, as it is in the model.

    Example: **`--name=user`**

```shell
   scaffold create:interface-resource --name=user --resource
```
